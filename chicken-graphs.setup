;; -*- Hen -*-

(compile -O3 -d0 -s chicken-graphs.scm -j graphs)
(compile -O2 -d0 -s graphs.import.scm)

(install-extension
 'graphs
 `(,(dynld-name "graphs") ,(dynld-name "graphs.import"))
 `(version 0.1)))
